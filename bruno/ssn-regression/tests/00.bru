meta {
  name: Test 0 (expects 303)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}
  body: none
  auth: none
}

headers {
  Accept: text/html,*/*;q=0.8
}

assert {
  res.status: eq 302
  res.headers.location: eq http://localhost:8080/ns/ssn/2023/
}

settings {
  encodeUrl: false
  followRedirects: false
  maxRedirects: 5
  timeout: inherit
}
