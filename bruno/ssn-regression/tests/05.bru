meta {
  name: Test 5 (expects 200)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/dul
  body: none
  auth: none
}

headers {
  Accept: text/html,*/*;q=0.8
}

assert {
  res.status: eq 200
  res.headers.content-type: eq "text/turtle; charset=utf-8"
}

settings {
  encodeUrl: false
  followRedirects: false
  maxRedirects: 5
  timeout: inherit
}
