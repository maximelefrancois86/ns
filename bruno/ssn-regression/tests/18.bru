meta {
  name: Test 18 (expects 303)
  type: http
  seq: 19
}

get {
  url: {{baseUrl}}/System
  body: none
  auth: none
}

headers {
  Accept: text/html
}

assert {
  res.status: eq 303
  res.headers.location: eq "https://www.w3.org/TR/vocab-ssn/#SSNSystem"
}

settings {
  encodeUrl: false
  followRedirects: false
  maxRedirects: 5
  timeout: inherit
}
