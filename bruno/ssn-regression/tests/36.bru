meta {
  name: Test 36 (expects 200)
  type: http
  seq: 37
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

headers {
  Accept: text/html,text/turtle
}

assert {
  res.status: eq 200
  res.headers.content-type: eq "text/turtle; charset=utf-8"
}

settings {
  encodeUrl: false
  followRedirects: false
  maxRedirects: 5
  timeout: inherit
}
