meta {
  name: Test 39 (expects 200)
  type: http
  seq: 40
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

headers {
  Accept: */*
}

assert {
  res.status: eq 200
  res.headers.content-type: eq "text/turtle; charset=utf-8"
}

settings {
  encodeUrl: false
  followRedirects: false
  maxRedirects: 5
  timeout: inherit
}
