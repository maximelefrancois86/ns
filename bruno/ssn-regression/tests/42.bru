meta {
  name: Test 41 (expects 200)
  type: http
  seq: 40
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

headers {
  Accept: text/turtle,text/html;qs=0.1
}

assert {
  res.status: eq 200
  res.headers.content-type: eq "text/turtle; charset=utf-8"
}

settings {
  encodeUrl: false
  timeout: inherit
  followRedirects: false
  maxRedirects: 5
}
